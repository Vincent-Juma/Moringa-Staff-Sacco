let amount, interestRate;
let total = 0;

class Gettotal {
  constructor(package, investment, time, total) {
    this.package = package;
    this.investment = investment;
    this.time = time;
    this.total = total;
  }
}







$(document).ready(function () {
  $('button.clear').trigger("reset")
});